# Production Environment Variables Template for TodoApp
#
# Copy this file to .env.production and customize values as needed.
#
# Note: These variables are embedded into the build at compile time.
# Changes require a rebuild to take effect.

# Build Information
# Automatically set by build script, but can be overridden
# VITE_BUILD_TIME=

# App Configuration
VITE_APP_NAME="Todo App"
VITE_APP_VERSION="1.0.0"

# API Endpoints (if applicable)
# VITE_API_BASE_URL=https://api.example.com
# VITE_API_TIMEOUT=30000

# Feature Flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG=false

# PWA Configuration
# These should match your manifest.ts configuration
VITE_PWA_THEME_COLOR="#7764E8"
VITE_PWA_BACKGROUND_COLOR="#171D34"

# External Services
# Add your production service keys here (if needed)
# VITE_GITHUB_CLIENT_ID=
# VITE_ANALYTICS_ID=

# Performance
VITE_ENABLE_SOURCE_MAPS=true
VITE_ENABLE_PWA=true

# Cache Configuration
# VITE_CACHE_VERSION=1

# Security
# Set to true in production for additional security checks
VITE_PRODUCTION_MODE=true

# Logging
VITE_LOG_LEVEL=error

# Network
# VITE_PEER_SERVER_HOST=
# VITE_PEER_SERVER_PORT=

# Remote Sync
VITE_REMOTE_URL=https://sync.example.com

# ============================================================================
# IMPORTANT NOTES:
# ============================================================================
# 1. Never commit sensitive keys or secrets to version control
# 2. All VITE_ prefixed variables are exposed to the client
# 3. Non-VITE_ variables are only available during build time
# 4. Use different values for development (.env.development) and production
# 5. Environment variables are embedded at build time, not runtime
# ============================================================================
